package day07;// 패키지명

public class Checker { // 클래스 시작
    boolean isEven(int x){
        if (x%2==0){
            return true;
        }else {
            return false;
        }
    }// 메소드 끝
}// 클래스 끝
